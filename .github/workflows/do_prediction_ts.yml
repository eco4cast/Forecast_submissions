on:
  workflow_dispatch:
  schedule:
  - cron: "0 20 * * *"
#  push:


jobs:
  build:
    runs-on: ubuntu-22.04
    container:
      image: eco4cast/rocker-neon4cast
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: install R packages
        run: R -e 'source("install.R")' 
# Point to the right path, run the right Rscript command
      - name: Run automatic prediction file (arima)
        run: Rscript ./Generate_forecasts/ARIMA/forecast_model.R
      - name: Run automatic prediction file (ets)
        run: Rscript ./Generate_forecasts/ETS/forecast_model.R
      - name: Run automatic prediction file (tbats)
        run: Rscript ./Generate_forecasts/TBATS/forecast_model.R
